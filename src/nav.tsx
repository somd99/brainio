import React, { FC } from "react";

const Nav: FC = () => {
  return (
    <div className="w-full">
      <header className="bg-teal-400">
        <nav className="flex justify-center w-full bg-white text-joy-purple2 text-2xl font-bold p-4">
            Overview
        </nav>
      </header>
    </div>
  );
};

export default Nav;
